
import { categoryFilter } from 'craigslist3/helpers/category-filter';
import { module, test } from 'qunit';

module('Unit | Helper | category filter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = categoryFilter([42]);
  assert.ok(result);
});

